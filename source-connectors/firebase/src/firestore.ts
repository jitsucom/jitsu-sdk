import { FirestoreStreamConfig } from "./index";
import { App} from "firebase-admin/app";
//import { getFirestore } from "firebase-admin/firestore";

export async function streamFirestore(firebaseApp: App, streamConfiguration: FirestoreStreamConfig) {
  //console.log(getFirestore(firebaseApp));
}
